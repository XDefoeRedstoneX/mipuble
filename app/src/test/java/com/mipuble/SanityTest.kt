package com.mipuble

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder test that proves the unit-test source set, JUnit, and CI wiring all work.
 * Real domain tests (starting with the natural-order comparator) arrive in Phase 1.
 */
class SanityTest {
    @Test
    fun arithmetic_holds() {
        assertEquals(4, 2 + 2)
    }
}
